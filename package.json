{
  "name": "io.chronobank.starter",
  "version": "1.0.35",
  "description": "",
  "main": "ecosystem.config.js",
  "scripts": {
    "build": "/bin/bash ./bin/build",
    "build:yarn": "/bin/bash ./bin/build-yarn",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "execa": "^0.8.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "simple-git": "^1.77.0"
  }
}
